/**
 * A material-ui icon rendered dynamically
 */

// import styled from 'styled-components';
import * as Icons from '@material-ui/icons';

const MaterialIcon = icon => Icons[icon];

export default MaterialIcon;
